#
# Copyright contributors to the Galasa project 
#
apiVersion: v1
kind: Service
metadata:
  name: binary-cli
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: binary-cli
